// Деструктурируем хуки React один раз для всех файлов (общий глобальный скоуп)
const { useState, useEffect } = React;

// ── Ключи LocalStorage ────────────────────────────────────
const STORAGE_KEY     = 'todo-app';   // ключ нового формата
const LEGACY_KEY      = 'todo-tasks'; // ключ старого формата (V1)
const DEFAULT_LIST_ID = 'default';

/**
 * Справочник приоритетов.
 * order — числовой вес для сортировки (меньше = выше в списке).
 */
const PRIORITIES = {
  high:   { label: 'Высокий', color: '#ef4444', order: 0 },
  medium: { label: 'Средний', color: '#f59e0b', order: 1 },
  low:    { label: 'Низкий',  color: '#22c55e', order: 2 },
};

// ── Мотивационные фразы ───────────────────────────────────
const MOTIV_PHRASES = [
  'Отлично! Так держать!',
  'Ещё одна задача закрыта!',
  'Ты молодец, продолжай!',
  'Прогресс очевиден!',
  'Один шаг к цели!',
  'Сделано — значит победа!',
];
